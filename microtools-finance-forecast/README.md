# microtools-finance-forecast

MVP web en React + TypeScript para estimar el ingreso neto anual a partir de un sueldo bruto mensual y tasas de retención.

## Run

```bash
npm install
npm run dev
```

## Crear repo privado en GitHub

```bash
gh repo create microtools-finance-forecast --private --source=. --remote=origin --push
```
